using System;
using UnityEngine;

public class _TEMPLATE_ : MonoBehaviour
{
    // Components (Naming Convention: _camelCase)

    // Properties
    /*
        [Serializable]
        struct Properties
        {
            public string name;
            public string description;
        }
        [SerializeField]
        Properties properties;
    */

    // Private (Naming Convention: camelCase)
    // Public (Naming Convention: camelCase)
    // Properties

    /*
        [SerializeField]
        bool _template = false;
        public bool Template
        {
            get { return _template; }
            private set { }
        }
    */

    // Unity Built-In Methods
    //private void Awake()
    //private void Start()
    //private void Update()
    //private void FixedUpdate()

    // Unity Event Methods
    // Getters And Setters
}
